
#Speed of BPMN resolution coefficient time (n : number of bpmn benchmarked at the same moment)
BENCH_POOL_SPEED: 3

#Number of blocks in seconds
BLOCK_PERIOD: 0

#Location of your SSH key for node access if needed
SSH_KEY: '/root/.ssh/id_rsa'

#Working dir used in each node to install Ethereum
NODE_WORKING_DIR: '/opt/benchmark'

NB_ACCOUNTS: {{ salt['pillar.get']('sc-benchmar:user_account') }}  
